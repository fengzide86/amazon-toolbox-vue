# 课赛通 KST · 先选对入口

源码只在当前文件夹 `D:\开发项目\amazon-toolbox-vue` 维护。以下入口都按自身位置定位项目，从桌面快捷方式或其他目录打开也有效；不需要复制项目或手动设置系统 PATH。

## 平时开发、体验和打包

| 你要做什么 | 打开哪个文件 | 会不会影响线上 |
| --- | --- | --- |
| 看管理员端，开发本地系统 | **开发预览.bat** | 不会；默认使用本地数据库与后端 |
| 看用户端 C/B 登录入口 | **dev-preview.bat** | 不会；C/B 由登录授权对应的产品权限决定 |
| 看独立宣传官网 | **官网预览.bat** | 不会；浏览器预览，与管理系统是两份构建产物 |
| 跑日常前后端检查 | **检查.bat** | 不会 |
| 生成当前源码的安装包 | **仅打包.bat** | 不会；输出 `release\KST Setup <版本>.exe`，不上传、不提交、不改版本 |

默认预览不是线上数据。首次本地管理员初始化会提示设置密码，不会替你创建或修改生产管理员。代理使用管理员登录入口中的代理账号，登录后进入自己的工作台；不要将拥有全部数据的管理员账号交给代理。

## 正式上线

| 发布目标 | 入口 | 范围 |
| --- | --- | --- |
| **系统和宣传官网一起更新** | **联合发布.bat** | 推荐的完整上线入口：先预检官网通道，发布系统，再发布独立官网 |
| 只更新系统 | **一键发布.bat** | 后端、业务 Web 应用、桌面安装包和自动更新；**不包含独立宣传官网** |
| 只更新宣传官网 | **官网发布.bat** | 不重发系统；系统已成功、官网失败时也用它恢复 |

发布器不会自动提交代码、合并分支、绕过 CI 或关闭校验。正式发布前必须准备**新的版本号、已提交并合入 `main` 的源码、同一提交通过的 CI，以及当前机器上的发布配置和登录**。不能把一个版本号重复用于不同安装包。

### 对外版本节奏

课赛通 KST 当前保持 **v1.x 产品主线**，现行技术版本为 `v1.8.10`。日常修复和体验加固继续使用补丁版本；兼容现有客户的功能迭代使用次版本（例如 `1.9.0`）。只有公共 API、数据结构、桌面身份或升级路径发生不兼容的破坏性变更，才评估 `2.0.0`。发布器不会因为功能数量增加就自动进入第二代；官网和日常客户端页面不展示技术版本，只有检测到更新时才在更新提示中显示本次版本，避免把仍在持续迭代的产品误解成“第二代产品”。

发布过程中会明确要求确认；不要为日常预览设置 `TOOLBOX_AUTO_PUBLISH=1`。预检失败或窗口报错不是发布成功，按错误处理后重试；系统中断恢复必须沿用原发布 ID，不要创建新的一轮同版本发布。

## 当前官网通道

2026-09-21 核验的公开官网为 [课赛通 KST 官网](https://fengzide86.github.io)，由独立 GitHub Pages 静态仓库承载。`kesaitong.pages.dev` 当时返回 522，不能将“Cloudflare 已登录”当作官网已经正常可用。`kesaitong.top` 仍需单独核验域名接入，不能默认已绑定。

以本机忽略提交的官网发布配置所选通道为准；无需把业务系统页面直接复制成宣传站，也不要手工把源码推到静态产物仓库。官网中的下载链接应由已正式发布的系统更新清单生成，避免长期写死旧版本。

## 常用命令（在当前文件夹打开 PowerShell）

```powershell
# 只核对本地预览/构建计划，不启动或部署
.\开发预览.bat --dry-run
.\dev-preview.bat --dry-run
.\仅打包.bat --dry-run
.\官网预览.bat --dry-run
.\联合发布.bat --dry-run

# 完整发布门禁，只检查与构建，不上线
.\检查.bat full

# 发布已经提交的新版本；这里自动读取，不硬编码旧版本
$kstVersion = (Get-Content -LiteralPath .\package.json -Raw | ConvertFrom-Json).version
.\联合发布.bat "--version=$kstVersion"
```

`一键发布.bat --dry-run` 与前述本地计划检查不同：它仍核验干净 Git、远端主分支与标签，但不会部署；没有通过不代表应跳过门禁。

## 机器迁移或入口报错

- 入口自动查找已有 Node 22、项目 Python 和 GitHub CLI，不自动安装、不修改系统 PATH。
- 如明确指定解释器或工具，使用当前进程的 `TOOLBOX_NODE_EXE`、`TOOLBOX_PYTHON`、`TOOLBOX_GH_EXE`；路径错误会直接报错，不静默换成别的工具。纯官网入口不需要 Python。
- 双击失败会保留错误窗口。自动化设置 `TOOLBOX_NO_PAUSE=1` 可免暂停，退出码仍保留。
- 详细前置条件、发布恢复和回滚见 [开发与发布说明](docs/DEVELOPMENT_RELEASE.md)。请勿在说明或 Git 中保存登录凭据。
